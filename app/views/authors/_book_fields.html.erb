
<p class="field">
	&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
	<%= f.label :title %>
	<%= f.text_field :title %>


	<%= f.fields_for :book_info do |info| %>
	  <%= render 'book_info_fields', :f=>info %>
	<%end%>
	 <%= link_to_add_fields "Add Book Info", f, :book_info %>
</p>
